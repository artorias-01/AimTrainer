import React from 'react';

/**
 * ArenaBackdrop3D
 * Note: Custom background imagery is rendered via CSS page background layer behind the
 * transparent WebGL canvas in ArenaScene.tsx. This component remains for optional future
 * 3D scene environment extensions.
 */
export const ArenaBackdrop3D: React.FC = () => {
  return null;
};
